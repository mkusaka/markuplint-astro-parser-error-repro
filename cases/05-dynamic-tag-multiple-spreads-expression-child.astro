---
const Element = 'button';
const iconOnly = true;
const label = 'close';
const rest = { id: 'close-button' };
---

<!--
Command:
  pnpm case:dynamic-tag-multiple-spreads

Expected:
  markuplint exits with parse-error.

Representative error:
  SyntaxError: Invalid tag syntax: "<Element
    {...iconOnly ? { title: label } : {}}
    {...rest}
  >
    {label}
  </Element>"

Direct parser comparison:
  pnpm inspect
  astro-eslint-parser accepts this file.

Scope note:
  Multiple spreads are not the only trigger. The smaller base failure is a spread
  attribute and an expression child on the same element; see cases/03a-static-spread-expression-child.astro.
-->
<Element
  {...iconOnly ? { title: label } : {}}
  {...rest}
>
  {label}
</Element>
