---
const foo = 'hello';
---

<!--
Command:
  pnpm case:ok-inline

Expected:
  Exit code 0.
  This is the baseline that shows explicit is:inline + define:vars passes.
-->
<script is:inline define:vars={{ foo }}>
  console.log(foo);
</script>
